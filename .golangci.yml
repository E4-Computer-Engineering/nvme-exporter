version: "2"
linters:
  default: all
  disable:
    - err113
    - exhaustruct
    - forbidigo
    - funlen
    - gochecknoglobals
    - ireturn
    - maintidx
    - mnd
  settings:
    depguard:
      rules:
        main:
          list-mode: lax
          files:
            - $all
          deny:
            - pkg: io/ioutil
              desc: 'replaced by io and os packages since Go 1.16: https://tip.golang.org/doc/go1.16#ioutil'
    varnamelen:
      min-name-length: 2
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - goconst
        path: (.+)_test\.go
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
  settings:
    gci:
      sections:
        - standard
        - default
        - localmodule
    goimports:
      local-prefixes:
        - github.com/E4-Computer-Engineering/nvme_exporter
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
